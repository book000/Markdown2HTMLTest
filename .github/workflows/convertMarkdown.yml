# This is a basic workflow to help you get started with Actions

name: Markdown to HTML

# Controls when the action will run. 
on:
  # Triggers the workflow on push or pull request events but only for the master branch
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
        with:
          ref: master
      - name: Install pandoc
        run: sudo apt install -y pandoc
      - name: Convert Markdown to HTML
        run: find -type f -name '*.md' | sed -e 's/\.md$//' | xargs -L 1 -I {} pandoc -f markdown -t html -o {}.html {}.md
      - name: Checkout branch & Commit files
        run: |
          find -type d -name '.git' -prune -o -type f -not -name '*.md' | xargs -L 1 -I {} rm "{}"
          git add *.html
          git checkout -b release
          git commit -v -m "[Bot] Auto build $(date +'%Y-%m-%d')"
          git push origin release
